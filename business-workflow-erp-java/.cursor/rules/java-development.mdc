---
description: 
globs: 
alwaysApply: true
---
# Java开发规范 - 危险废物处理企业ERP管理系统
## 本项目技术栈
- **框架**: Spring Boot 2.7+ / 3.x
- **安全框架**: Spring Security
- **ORM框架**: MyBatis-Plus
- **数据库**: MySQL 8.0+
- **缓存**: Redis
- **消息队列**: RabbitMQ / RocketMQ（可选，用于异步消息推送）
- **文件存储**: 本地存储 / OSS（阿里云OSS/腾讯云COS）
- **工具库**: 
  - Lombok（简化代码）
  - Hutool（工具类库）
  - Jackson（JSON处理）
  - Apache POI（Excel处理）
  - iText / PDFBox（PDF处理）
  - Tesseract OCR（OCR识别，可选）
## 核心原则
- 严格遵循 **SOLID、DRY、KISS、YAGNI** 原则
- 遵循 **OWASP 安全最佳实践**（输入验证、SQL注入防护）
- 采用 **分层架构设计**，确保职责分离
- 代码变更需通过 **单元测试覆盖**（测试覆盖率 ≥ 80%）
- 每次写完所有的代码都要进行build操作没确认是否有语法错误
## 项目架构规范

### 项目结构
```
src/main/java/com/erp/
├── config/                    # 配置类
│   ├── SecurityConfig.java   # Spring Security配置
│   ├── MybatisPlusConfig.java # MyBatis-Plus配置
│   ├── RedisConfig.java      # Redis配置
│   ├── CorsConfig.java       # 跨域配置
│   └── WebMvcConfig.java     # Web MVC配置
├── common/                    # 公共模块
│   ├── constant/             # 常量定义
│   ├── enums/                # 枚举类
│   ├── exception/            # 异常处理
│   ├── result/               # 统一响应结果
│   ├── util/                 # 工具类
│   └── annotation/           # 自定义注解
├── security/                 # 安全模块
│   ├── filter/               # 过滤器
│   ├── handler/               # 处理器（登录成功/失败）
│   ├── provider/              # 认证提供者
│   └── token/                 # Token管理
├── controller/                # 控制器层
│   ├── auth/                 # 认证控制器
│   ├── customer/             # 客户管理控制器
│   ├── contract/             # 合同管理控制器
│   ├── production/            # 生产执行控制器
│   ├── finance/               # 财务管理控制器
│   ├── system/                # 系统管理控制器
│   └── report/                # 报表控制器
├── service/                   # 服务层
│   ├── auth/                 # 认证服务
│   ├── customer/             # 客户管理服务
│   ├── contract/             # 合同管理服务
│   ├── production/            # 生产执行服务
│   ├── finance/               # 财务管理服务
│   ├── system/                # 系统管理服务
│   └── report/                # 报表服务
├── mapper/                    # 数据访问层
│   ├── customer/             # 客户管理Mapper
│   ├── contract/             # 合同管理Mapper
│   ├── production/            # 生产执行Mapper
│   ├── finance/                # 财务管理Mapper
│   ├── system/                # 系统管理Mapper
│   └── report/                # 报表Mapper
├── entity/                    # 实体类（对应数据库表）
│   ├── customer/             # 客户相关实体
│   ├── contract/             # 合同相关实体
│   ├── production/            # 生产执行相关实体
│   ├── finance/               # 财务相关实体
│   ├── system/                # 系统管理相关实体
│   └── common/                # 公共实体
├── dto/                       # 数据传输对象
│   ├── request/              # 请求DTO
│   └── response/             # 响应DTO
├── vo/                        # 视图对象
├── aspect/                    # 切面（AOP）
│   ├── LogAspect.java        # 日志切面
│   ├── PermissionAspect.java # 权限切面
│   └── DataHashAspect.java   # 数据Hash切面
└── Application.java           # 启动类
```

### 分层职责规范

#### Controller层 (`controller`)
- **职责边界**: 接收HTTP请求，参数校验，调用Service层，**禁止编写业务逻辑**
- **调用规范**: 只能调用Service层方法
- **API规范**: 
  - 请求方法：GET（查询）、POST（新增）、PUT（更新）、DELETE（删除）
  - URL规范：`/api/{module}/{resource}/{id}`
  - 参数规范：超过2个参数必须定义DTO实体
  - 响应格式：统一响应格式
    ```json
    {
      "code": 200,
      "message": "成功",
      "data": {},
      "timestamp": "2025-01-01 12:00:00"
    }
    ```
- **路径规范**: 
  - 认证模块: `/api/auth/`
  - 客户管理: `/api/customer/`
  - 合同管理: `/api/contract/`
  - 生产执行: `/api/production/`
  - 财务管理: `/api/finance/`
  - 系统管理: `/api/system/`
  - 报表: `/api/report/`

#### Service层 (`service`)
- **职责**: 业务逻辑处理，事务管理，调用Mapper层
- **设计原则**: 一个业务领域一个Service，一个功能一个方法
- **调用规范**: Service 层调用 Mapper 层进行数据库操作，使用 Redis 进行缓存操作
- **数据类型**: 入参和返参使用DTO类型，避免直接使用实体类
- **事务管理**: 使用 `@Transactional` 注解管理事务

#### Mapper层 (`mapper`)
- **职责**: 数据库操作，SQL映射
- **技术选型**: 优先使用MyBatis-Plus单表CRUD，复杂查询使用XML
- **调用规范**: Mapper 接口只能被 Service 层调用
- **接口规范**: 必须继承`com.baomidou.mybatisplus.core.mapper.BaseMapper<Entity>`
- **文件规范**: XML文件命名为`表名+Mapper.xml`，存放在`src/main/resources/mapper/`
- **数据类型**: 入参使用Entity或自定义DO对象，如果返回的是整表的字段可以使用Entity，如果有多表联合字段，需要定义DO对象，禁止自定义Map类型
- **注意**: 所有SQL必须写到mapper.xml文件中，不允许在mapper方法中写SQL语句


## 通用工具使用规范

### 分页处理
- 使用MyBatis-Plus分页插件进行分页查询
- 分页参数使用Page对象，返回IPage对象
- 分页配置在MybatisPlusConfig中统一配置

### 金额处理
- 所有金额使用BigDecimal类型，确保精度
- 数据库存储：使用DECIMAL类型
- 前端展示：元为单位（BigDecimal）
- **严禁**: 使用float和double进行金额计算

### 用户信息获取
- 获取当前登录用户信息通过Spring Security的SecurityContext获取
- 支持获取用户ID、用户名、角色、权限等信息
- Service层和Controller层中可以直接使用

### Redis缓存
- Redis操作使用Spring Data Redis或RedisTemplate
- 所有Redis key维护在常量类中
- 缓存命名规范：`模块名_功能名_标识符`
- 缓存时效：
  - 登录token缓存：2小时（JWT Token），7天（Refresh Token）
  - 验证码缓存：5分钟
  - 权限信息缓存：根据业务需求设置
  - 字典数据缓存：根据业务需求设置

### Hutool工具库优先
- 字符串操作：`StrUtil` 替代 `StringUtils`
- 集合操作：`CollUtil` 处理集合相关操作
- 时间处理：`DateUtil` 进行时间格式化和计算
- UUID生成：`IdUtil.fastUUID()`

## 数据处理规范

### DTO规范
- **命名规范**: 功能明确命名，如`AdminUserCreateDTO`、`MallUserLoginDTO`
- **字段校验**: 使用Bean Validation注解进行参数校验（`@NotBlank`, `@Size`, `@Email`等）
- **分类存放**: 按功能分包存放（user/、shop/、goods/等）
- **对象转换**: **禁止使用`BeanUtil.copyProperties()`**，采用手动字段赋值
- **转换封装**: DTO转换方法统一放在各模块convert包中

### 枚举和状态管理
- 所有状态字段使用枚举类型，避免魔法数字
- 枚举定义在对应模块的enums包中
- 状态变更通过枚举方法进行校验

### Entity赋值规范
- 创建或更新 Entity 对象时，必须为所有必填字段逐一显式赋值，任何字段不得遗漏  
- 建议使用 **Builder 模式** 或全参构造函数确保字段完整  
- **严禁** 使用 `BeanUtil.copyProperties()` 或类似"快速复制"方式，必须手动赋值以增强可读性与可控性  
- 代码评审时应重点检查 Entity 字段是否全部赋值，防止空值写入数据库

## 安全规范

### 密码安全
- 密码存储必须使用BCrypt加密：`BCryptPasswordEncoder.encode(password)`
- 登录验证时使用BCrypt比对：`BCryptPasswordEncoder.matches(rawPassword, encodedPassword)`
- 重置密码时重新加密存储

### 数据验证
- 所有外部输入必须验证
- 使用参数化查询防SQL注入
- 对输出内容进行适当转义防XSS

### 认证授权
- 使用Spring Security进行认证和授权
- JWT Token认证机制
- 基于角色的访问控制（RBAC）+ 自定义权限
- Token存储在Redis中，用于快速验证和强制下线

## 异常处理规范

### 异常分类
- `BusinessException`: 业务逻辑异常
- `ServiceException`: 服务层处理异常  
- `DataAccessException`: 数据访问异常

### 异常处理原则
- 所有异常catch必须记录详细日志（堆栈、参数、方法信息）
- Service层异常由统一异常处理器处理
- **事务异常**: 在`@Transactional`函数中不要使用`try...catch`捕获异常

## 严格禁止事项

1. **文件修改**: 禁止修改`pom.xml`（未经允许）和实体类文件
2. **对象拷贝**: 禁止使用`BeanUtil.copyProperties()`
3. **循环查询**: 禁止在循环中查询数据，应该使用批量查询
4. **包名调用**: 不要使用完整包名调用类或方法
5. **数据库变更**: 必须生成SQL文档存放在`doc/`目录
6. **方法实现** : 禁止生成空的方法或者使用测试、默认的值，如果有请在生成完会后告知我

## 开发流程规范

### 标准开发流程
1. **需求分析**: 查阅 [README.md](mdc:README.md) 确认是否有相似功能
2. **架构设计**: 确定核心逻辑实现层级，避免重复开发
3. **Mapper层**: 定义数据访问接口，包括常用的正删改查，根据ID或者IDS获取数据，根据Entity 来设置condition查询数据
4. **Service层**: 实现具体业务流程
5. **Controller层**: 提供HTTP接口，仅做参数校验和路由

### 代码复用原则
- 开发前必查@README中的service说明
- 发现重复代码时，及时重构到合适层级

### 代码质量要求
- 每个方法添加Javadoc注释
- 方法行数不超过300行
- 使用描述性的变量、函数和类名
- 保持代码简洁明了，遵循KISS原则
- 

## 性能优化规范

1. **数据库操作**:
   - 避免在循环中频繁查询数据库
   - 使用批量操作代替逐个操作
   - 复杂查询优化SQL性能
   - 不要在service层或者manager循环中调用访问数据库的方法，尽量使用批量查询，批量修改，批量插入，比如下面的错误示例
```
 for (Integer skuId : skuIds) {
            List<GylSsu> ssuList = gylSsuManager.getSsuListBySkuId(skuId);
            ssuListMap.put(skuId, ssuList);
        }
```
 正确写法：
 ```
 List<GylSsu> ssuList = gylSsuManager.getSsuListBySkuIds(skuIds);
 ```

2. **缓存策略**:
   - 避免在事务中进行Redis操作
   - 合理设置缓存过期时间
   - 及时清理无效缓存

3. **内存优化**:
   - 及时释放大对象引用
   - 避免创建不必要的临时对象

## 最佳实践总结

### 关注点分离
- Gateway层专注请求处理
- Service层专注业务逻辑  
- Mapper层专注数据访问

### 可维护性
- 使用枚举管理状态
- 使用DTO传输数据
- 手动字段赋值提高可读性
- 完善的异常处理和日志记录
