---
description: 
globs: 
alwaysApply: true
---
---
description: 代码生成前业务服务检查规则
ruleType: always
alwaysApply: true
---

# 业务代码生成检查规则

## 核心原则
**严格要求：在生成任何业务代码之前，AI必须主动检查是否已有相关的业务服务或功能。**

## 强制检查流程
### 第一步：必须主动检查现有代码
- **强制要求**：检查 src/main/java/com/erp 目录下的相关模块
- **检查范围**：Service层、Mapper层、Entity层、枚举类
- **检查目的**：确认是否已有相关功能或类似方法

### 第二步：逐层分析现有功能
1. **服务层检查**: 查看 `service` 目录，确认是否已有类似的业务服务
2. **Mapper层检查**: 查看 `mapper` 目录，确认是否已有相关的数据访问接口
3. **实体类检查**: 查看 `entity` 目录，确认是否已有相关的实体类
4. **枚举类检查**: 查看 `common/enums` 目录，确认是否已有相关的枚举定义

### 第三步：代码复用决策
- **优先级1**：如果发现已有完全匹配的功能，直接复用现有代码
- **优先级2**：如果发现已有类似功能，在现有类中扩展方法
- **优先级3**：如果发现相关但不完全匹配的功能，评估是否可以重构复用
- **最后选择**：只有在确认没有任何相关功能时，才创建新的类或服务

## 具体检查示例
### 客户相关功能开发
- **必查项目**：CustomerService、CustomerMapper的所有方法
- **检查内容**：客户CRUD、状态管理、关联查询等
- **复用策略**：优先使用现有客户服务，避免创建新的客户管理功能

### 合同相关功能开发
- **必查项目**：ContractService、ContractMapper的所有方法
- **检查内容**：合同创建、审核、状态管理、执行跟踪等
- **复用策略**：合同相关操作必须使用现有服务

### 员工相关功能开发
- **必查项目**：EmployeeService、EmployeeMapper等
- **检查内容**：员工管理、角色分配、权限管理等
- **复用策略**：员工相关功能必须基于现有服务扩展

## 违规检查机制
### 发现重复开发的处理
1. **立即停止**：一旦发现已有相关功能，立即停止新开发
2. **重新分析**：分析现有功能是否满足需求
3. **选择策略**：确定是复用、扩展还是重构
4. **重新实施**：基于选定策略重新实施

### 检查不充分的后果
- **代码冗余**：造成系统中存在重复功能
- **维护困难**：增加后续维护和bug修复的复杂度
- **性能问题**：重复的数据库查询和业务逻辑
- **团队效率下降**：浪费开发时间和资源

## 命名规范参考
- Service层：`{业务域}Service`，如 `CustomerService`、`ContractService`
- Mapper层：`{业务域}Mapper`，如 `CustomerMapper`、`ContractMapper`
- Entity层：`{业务域}`，如 `Customer`、`Contract`

## 文档同步要求
- **每次新增服务**：必须同步更新相关文档
- **每次修改方法**：必须同步更新相关服务的功能描述
- **每次重构**：必须更新相关的文档说明

## 执行检查清单
在开始任何代码开发前，请确认：
- [ ] 已检查 src/main/java/com/erp 目录下的相关模块
- [ ] 已逐层检查Service、Mapper、Entity、枚举层
- [ ] 已确认没有重复或类似的现有功能
- [ ] 已选择合适的复用或扩展策略
- [ ] 已准备在完成后更新相关文档

**重要提醒：违反此检查流程将导致代码重复、系统冗余和维护困难，必须严格遵守！**
