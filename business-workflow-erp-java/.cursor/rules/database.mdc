---
description: 
globs: 
alwaysApply: false
---
# 数据库实体规范
需要先扫描 src/main/java/com/erp/entity 下是否有存在的实体，如果存在则直接使用

## 实体类命名规范
- 实体类使用PascalCase命名，如 `Customer`、`Contract`、`Employee`
- 实体类对应数据库表，表名使用下划线命名，如 `customer`、`contract`、`employee`

## 实体类要求
- 所有实体类必须继承MyBatis-Plus的BaseEntity（如需要）
- 使用Lombok注解简化代码（@Data、@Builder等）
- 使用MyBatis-Plus注解（@TableName、@TableId等）
- 字段使用驼峰命名，对应数据库下划线字段

## 数据库设计规范
- 使用MySQL 8.0+
- 表名使用下划线命名
- 主键使用自增ID或UUID
- 支持逻辑删除（deleted字段）
- 自动填充创建时间、更新时间等字段