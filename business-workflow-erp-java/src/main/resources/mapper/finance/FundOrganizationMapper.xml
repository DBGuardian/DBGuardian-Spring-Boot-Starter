<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erp.mapper.finance.FundOrganizationMapper">

    <!-- 资金组织分页查询（关联创建人/更新人），支持模糊查询与字段排序 -->
    <select id="selectFundOrganizationPage" resultType="com.erp.controller.finance.dto.FundOrganizationListItemResponse">
        SELECT
            fo.`组织编号`       AS organizationId,
            fo.`组织编码`     AS organizationCode,
            fo.`组织名称`     AS organizationName,
            fo.`描述`           AS description,
            fo.`是否启用`               AS enabled,
            fo.`创建时间`            AS createTime,
            fo.`更新时间`            AS updateTime,
            COALESCE(acc_stats.account_count, 0) AS accountCount,
            COALESCE(acc_stats.total_balance, '0.00') AS totalBalance,
            e1.`员工姓名`            AS createUserName,
            e2.`员工姓名`            AS updateUserName,
            fo.`创建人编码`          AS creatorId
        FROM fund_organization fo
                 LEFT JOIN EMPLOYEE e1 ON fo.`创建人编码` = e1.`员工编码`
                 LEFT JOIN EMPLOYEE e2 ON fo.`更新人编码` = e2.`员工编码`
                 LEFT JOIN (
            SELECT
                fa.`组织编号` AS organization_id,
                COUNT(fa.`账户编号`) as account_count,
                '0.00' as total_balance
            FROM fund_account fa
            WHERE fa.`组织编号` IS NOT NULL
            GROUP BY fa.`组织编号`
        ) acc_stats ON fo.`组织编号` = acc_stats.organization_id
        <where>
            <if test="query.organizationName != null and query.organizationName != ''">
                AND fo.`组织名称` LIKE CONCAT('%', #{query.organizationName}, '%')
            </if>
            <if test="query.enabled != null">
                AND fo.`是否启用` = #{query.enabled}
            </if>
            <!-- viewScope=SELF 时只查自己创建的组织 -->
            <if test="query.creatorFilter != null">
                AND fo.`创建人编码` = #{query.creatorFilter}
            </if>
        </where>
        <choose>
            <when test="query.sortField != null and query.sortField != ''">
                ORDER BY
                <choose>
                    <when test="query.sortField == 'organizationCode'">
                        fo.`组织编码`
                    </when>
                    <when test="query.sortField == 'organizationName'">
                        fo.`组织名称`
                    </when>
                    <when test="query.sortField == 'createTime'">
                        fo.`创建时间`
                    </when>
                    <when test="query.sortField == 'updateTime'">
                        fo.`更新时间`
                    </when>
                    <otherwise>
                        fo.`创建时间`
                    </otherwise>
                </choose>
                <choose>
                    <when test="query.sortOrder != null and (query.sortOrder == 'asc' or query.sortOrder == 'ASC')">
                        ASC
                    </when>
                    <otherwise>
                        DESC
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                ORDER BY fo.`创建时间` DESC
            </otherwise>
        </choose>
    </select>

    <!-- 根据组织ID查询组织详情 -->
    <select id="selectFundOrganizationDetail" resultType="com.erp.controller.finance.dto.FundOrganizationListItemResponse">
        SELECT
            fo.`组织编号`       AS organizationId,
            fo.`组织编码`     AS organizationCode,
            fo.`组织名称`     AS organizationName,
            fo.`描述`           AS description,
            fo.`是否启用`               AS enabled,
            fo.`创建时间`            AS createTime,
            fo.`更新时间`            AS updateTime,
            COALESCE(acc_stats.account_count, 0) AS accountCount,
            COALESCE(acc_stats.total_balance, '0.00') AS totalBalance,
            e1.`员工姓名`            AS createUserName,
            e2.`员工姓名`            AS updateUserName
        FROM fund_organization fo
                 LEFT JOIN EMPLOYEE e1 ON fo.`创建人编码` = e1.`员工编码`
                 LEFT JOIN EMPLOYEE e2 ON fo.`更新人编码` = e2.`员工编码`
                 LEFT JOIN (
            SELECT
                fa.`组织编号` AS organization_id,
                COUNT(fa.`账户编号`) as account_count,
                '0.00' as total_balance
            FROM fund_account fa
            WHERE fa.`组织编号` IS NOT NULL
            GROUP BY fa.`组织编号`
        ) acc_stats ON fo.`组织编号` = acc_stats.organization_id
        WHERE fo.`组织编号` = #{organizationId}
    </select>

    <!-- 查询所有组织（用于构建树形结构，包含账户数量） -->
    <select id="selectAllOrganizations" resultType="com.erp.controller.finance.dto.FundOrganizationListItemResponse">
        SELECT
            fo.`组织编号` AS organization_id,
            fo.`组织编码` AS organization_code,
            fo.`组织名称` AS organization_name,
            fo.`描述` AS description,
            fo.`是否启用` AS enabled,
            fo.`创建时间` AS created_at,
            fo.`更新时间` AS updated_at,
            fo.`创建人编码` AS create_user_id,
            fo.`更新人编码` AS update_user_id,
            fo.`version` AS version,
            COALESCE(account_stats.account_count, 0) AS accountCount
        FROM fund_organization fo
        LEFT JOIN (
            SELECT
                `组织编号`,
                COUNT(`账户编号`) as account_count
            FROM fund_account
            WHERE `组织编号` IS NOT NULL
            GROUP BY `组织编号`
        ) account_stats ON fo.`组织编号` = account_stats.`组织编号`
        WHERE fo.`是否启用` = true
        ORDER BY fo.`组织名称` ASC
    </select>

    <!-- 根据组织ID查询关联的账户列表（直接从账户表查询） -->
    <select id="selectAccountsByOrganizationId" resultType="com.erp.controller.finance.dto.FundAccountListItemResponse">
        SELECT
            fa.账户编号 AS accountId,
            fa.账户编码 AS accountCode,
            fa.账户名称 AS accountName,
            fa.账户类型 AS accountType,
            fa.账户银行账号 AS accountBankAccount,
            fa.账户银行机构 AS accountBankInstitution,
            fa.是否启用 AS enabled,
            fa.备注 AS remark,
            fa.创建时间 AS createTime,
            fa.更新时间 AS updateTime,
            e1.`员工姓名` AS createUserName,
            e2.`员工姓名` AS updateUserName
        FROM fund_account fa
        LEFT JOIN EMPLOYEE e1 ON fa.创建人编码 = e1.`员工编码`
        LEFT JOIN EMPLOYEE e2 ON fa.更新人编码 = e2.`员工编码`
        WHERE fa.`组织编号` = #{organizationId}
        ORDER BY fa.账户名称 ASC
    </select>


</mapper>
